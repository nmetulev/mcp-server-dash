# Copy this file to `.env` and fill in your Dropbox app credentials.
# Do not commit the real `.env` to source control.

# Dropbox OAuth app credentials
APP_KEY=your_dropbox_client_id
APP_SECRET=your_dropbox_client_secret

